/**
 * Axiom Shield - Popup Logic
 */
document.addEventListener('DOMContentLoaded', () => {
    console.log("Axiom Shield Popup loaded.");
    // Currently, we just show a static "Active" status per requirements.
    // In future versions, we could add toggles here.
});
